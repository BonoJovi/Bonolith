/target
/fcitx5/build

# Local Claude Code settings
/.claude/settings.local.json

# Stray runtime database (created when running from the repo root)
/data/bonolith.db

# Python bytecode cache from helper scripts
scripts/__pycache__/
*.pyc

# Work directory (temporary files, not for commit)
/work/
